fn main() {
    //Cuando importamos una libreria externa, podemos usar los metodos a traves de la sintaxis :: por cada nivel que necesitamos bajar hasta el metodo deseado.
    numbers::say_hello();

    numbers::print_num(5)
}
